Look at this screenshot of a macOS desktop. I need you to find the following UI element:

{{element_description}}

If you can see this element, respond with its coordinates in the format:
FOUND: x=<number>, y=<number>

If you cannot find this element, respond with:
NOT_FOUND

Only respond with one of these two formats, nothing else.
